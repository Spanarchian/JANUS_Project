import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Janus_Mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
